# Cowsay as a Service (CaaS)

Simple FastAPI app for cowsay in the browser.

## Usage

1. Install requirements:
   ```
   pip install -r requirements.txt
   ```
2. Run locally:
   ```
   uvicorn main:app --reload
   ```
3. Open [http://localhost:8000/landing](http://localhost:8000/landing)
